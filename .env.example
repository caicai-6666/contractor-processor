# 本地 vLLM 的 OpenAI 兼容接口通常不校验真实密钥，但不得在仓库提交实际值。
VLLM_MLLM_API_KEY=replace-with-local-token
VLLM_EMBEDDING_API_KEY=replace-with-local-token
VLLM_RERANKER_API_KEY=replace-with-local-token
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=REPLACE_WITH_ELASTICSEARCH_PASSWORD
# JSON 数组；用于从终审合同主体中排除我方，元素可以是完整名称或稳定简称。
CONTRACT_PROCESSOR_OWN_COMPANY_NAMES=["现象光伏","现象创新","phenosolar"]
