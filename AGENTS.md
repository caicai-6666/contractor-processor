# 项目开发规范

本文件适用于在本项目中进行的所有代码开发、修改和功能扩展。

## 开发前必读

在开始任何正式开发工作前，必须先阅读 [`description/PROJECT.md`](description/PROJECT.md)。

该文件说明项目的整体架构、模块边界及文档索引，可用于判断实现位置并定位相关资料。若文档目录或文件暂未创建，应先与需求方确认或补充基础项目说明，避免在不了解项目结构的情况下直接开发。

## 代码要求

- 在业务规则、关键流程、复杂逻辑、边界处理和设计取舍处添加清晰、准确的中文注释；避免为显而易见的代码重复注释。
- 代码应遵循合适的设计模式和 SOLID 等设计原则，保持职责单一、模块低耦合、接口清晰、易于测试与扩展。
- 不为套用模式而过度设计；应根据实际复杂度选择恰当的抽象，确保实现简洁、优雅且可维护。

## 功能文档要求

每完成一个功能模块的开发，必须及时在 `description/` 下对应的目录中新建或更新 Markdown 描述文档。

文档至少应包含：模块用途、主要职责、对外接口或使用方式、关键实现与设计决策、依赖关系，以及必要的配置或注意事项。文档应与代码同步维护。

## 实验分析日志

当需求方要求分析某次实验时，除在对话中给出结论外，必须将完整分析追加写入该次实验输出目录下的 `analysis.md`。

- 每个实验运行目录只维护一个 `analysis.md`；文件不存在时创建，已存在时只追加新章节，不覆盖或改写历史分析。
- 每次分析使用带时区的时间作为二级标题，并至少记录：分析范围、结论摘要、关键证据、原因判断、建议措施和验证状态。
- 新建文件或追加章节时遵循 [`experiments/EXPERIMENT_ANALYSIS_TEMPLATE.md`](experiments/EXPERIMENT_ANALYSIS_TEMPLATE.md) 的结构；可按实验特点增减三级小节，但不得省略关键证据和验证状态。
- 关键判断必须链接到同目录中的原始响应、指标、manifest 或校验产物；明确区分“产物可直接观察的事实”和“基于事实的推断”。
- 不在日志中复制大段原始模型输出；优先使用表格、计数、比例和简短片段呈现证据，保证文档易读。
- 若同一请求比较多次实验，应在每个被分析的运行目录中追加与该运行相关的结论，并通过相对链接引用其他运行目录。
- `analysis.md` 是人工分析记录，不由实验运行器自动生成，也不得修改同目录中的原始实验产物。
